<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:focusable="true"
    android:focusableInTouchMode="true">

    <EditText
        android:id="@+id/main_search_edit"
        android:layout_width="260dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="150dp"
        android:background="@drawable/login_edit_bg"
        android:padding="6dp" />

    <Button
        android:id="@+id/main_search_btn"
        android:layout_width="80dp"
        android:layout_height="40dp"
        android:layout_alignTop="@+id/main_search_edit"
        android:layout_marginLeft="16dp"
        android:layout_toRightOf="@+id/main_search_edit"
        android:background="@drawable/login_btn_bg"
        android:text="查询"
        android:textColor="@color/colorAccent" />
    
    <TextView
        android:id="@+id/main_hosptial_name"
        android:textColor="@color/gray"
        android:text="浙江省精神卫生中心VR康复室"
        android:gravity="center"
        android:layout_below="@+id/main_search_edit"
        android:textSize="20dp"
        android:layout_width="match_parent"
        android:layout_height="60dp" />
</RelativeLayout>